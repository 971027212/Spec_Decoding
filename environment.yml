name: specd
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip>=24.0
  - pip:
      - rich
      - tqdm
      - termcolor
      - numpy>=1.24.0
      - tokenizers==0.19.1
      - torch==2.3.0
      - transformers==4.41.1
      - accelerate==0.30.1
      - bitsandbytes==0.43.1
      - matplotlib>=3.7.0
